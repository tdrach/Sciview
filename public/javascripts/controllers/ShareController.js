app.controller('ShareController', [
    '$scope',
    '$rootScope',
    '$location',
    '$state',
    function($scope, $rootScope, $location, $state) {
        // $scope.$state = $state;
        $scope.share = {
          link: "app.sciview.com/3fj309kd39"
        };
    }
]);
